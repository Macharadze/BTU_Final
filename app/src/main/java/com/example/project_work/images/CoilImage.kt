package com.example.project_work.images

@Deprecated(
    message = "Compose/Coil composables removed in XML migration. Use ImageView.setImageResource(getWeatherIcon(code)).",
    level = DeprecationLevel.ERROR
)
private const val _removed = 0
